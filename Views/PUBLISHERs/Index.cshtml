@model IEnumerable<WebApplication1.Models.PUBLISHER>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PUBLISHERLINK)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PUBLISHERLINK)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PUBLISHERNAME }) |
            @Html.ActionLink("Details", "Details", new { id=item.PUBLISHERNAME }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PUBLISHERNAME })
        </td>
    </tr>
}

</table>
